const { fallbackData } = require('./dist/assets/index-BLToQeRT.js'); // Use the built file if possible, or just mock
// Actually, easier to just check the file content again with a very specific grep
